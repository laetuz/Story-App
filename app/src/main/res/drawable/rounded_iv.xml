<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="10dp" /> <!-- Change this value to adjust the roundness of the corners -->
    <solid android:color="@android:color/white" /> <!-- Change this value to adjust the background color -->
    <stroke android:width="2dp" android:color="@color/black"/>
</shape>